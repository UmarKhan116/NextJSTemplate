/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.dummyjson.com','avatars.githubusercontent.com','tailwindui.com'],
      },
}

module.exports = nextConfig
